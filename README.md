# Ostranauts-recaVentPowered

Adds a version of the Auto-Vent that doesn't automatically close.

Note that this item is not currently on any vendors. To get one, hit F3, enter `unlockdebug` and then `spawn ItmVentPoweredLoose`.
